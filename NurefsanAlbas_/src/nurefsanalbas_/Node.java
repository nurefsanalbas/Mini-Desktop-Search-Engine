package nurefsanalbas_;

/**
 *
 * @author nurefsanalbas
 */
public class Node {

    String data;//That holds binary search trees node's data.
    Node left, right;// We can traversal with node.left and node.right
    int count = 0;//That holds count of every element in binary search tree.

    public Node(String data) {
        this.data = data;
    }

}
